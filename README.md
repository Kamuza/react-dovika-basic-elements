# react-dovika-basic-elements

Basic project components
## Usage

Run the following command to install in yout reactjs project:

```
yarn add react-dovika-basic-elements
```
or
```
npm install react-dovika-basic-elements
```

